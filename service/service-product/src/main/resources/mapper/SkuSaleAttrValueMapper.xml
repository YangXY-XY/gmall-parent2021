<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper SYSTEM "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atguigu.gmall.product.mapper.SkuSaleAttrValueMapper">
    <resultMap id="spuValueIdsMap" type="java.util.Map" autoMapping="true">
    </resultMap>
    <select id="selectSaleAttrValuesBySpu" resultMap="spuValueIdsMap">
        select skv.sku_id,group_concat(sale_attr_value_id
            order by spv.base_sale_attr_id asc separator '|') value_ids
        from spu_sale_attr_value spv inner join sku_sale_attr_value skv
        on spv.id=skv.sale_attr_value_id
        where spv.spu_id=#{spuId}
        group by skv.sku_id;
    </select>
</mapper>